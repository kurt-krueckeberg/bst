functor(const Node *peekNode)
{

  // Get unique_ptr for peekNode 
  Node *parent = peekNode->parent;  
  
  if (!parent)
      
      visit(root);
      
  else {
     std::unique_ptr<Node>& ref = parent->left.get() == peekNode ? parent->left : parent->right;     
     visit(ref);
  }
}
